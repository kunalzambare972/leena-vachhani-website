// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
// Filter by degree and year
function filterStudents(degree) {
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemDegree = item.getAttribute("data-degree");
    if (degree === "all" || itemDegree === degree) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}

// Filter by year
function filterByYear() {
  const selectedYear = document.getElementById("year-select").value;
  const studentItems = document.querySelectorAll(".student-item");
  studentItems.forEach((item) => {
    const itemYear = item.getAttribute("data-year");
    if (selectedYear === "all" || itemYear === selectedYear) {
      item.style.display = "block"; // Show student item
    } else {
      item.style.display = "none"; // Hide student item
    }
  });
}
